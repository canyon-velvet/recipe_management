<nav class="bg-white border-b border-gray-200">
  <div class="container mx-auto px-5 py-3 flex items-center justify-between">
    <div class="flex items-center space-x-6">
      <%= link_to "食谱管理", root_path, class: "text-lg font-bold text-gray-900" %>
      <% if logged_in? %>
        <%= link_to "食谱", recipes_path, class: "text-gray-600 hover:text-gray-900" %>
        <%= link_to "餐计划", meal_plans_path, class: "text-gray-600 hover:text-gray-900" %>
      <% end %>
    </div>
    <div class="flex items-center space-x-4">
      <% if logged_in? %>
        <span class="text-sm text-gray-500"><%= current_user.username %></span>
        <%= link_to "退出", logout_path, data: { turbo_method: :delete }, class: "text-sm text-red-600 hover:text-red-800" %>
      <% end %>
    </div>
  </div>
</nav>
