<% flash.each do |type, message| %>
  <div class="<%= type == 'notice' ? 'bg-green-50 border-green-300 text-green-700' : 'bg-red-50 border-red-300 text-red-700' %> border rounded-md p-3 mb-4">
    <%= message %>
  </div>
<% end %>
